/**
 * CategoryManager.js - CategoryManager
 * Stub temporário
 */

(function(window) {
    'use strict';

    const KC = window.KnowledgeConsolidator;

    class CategoryManager {
        constructor() {
            // Stub
        }

        initialize() {
            console.log('CategoryManager inicializado (stub)');
        }
    }

    KC.CategoryManager = new CategoryManager();

})(window);
