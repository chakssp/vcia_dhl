/**
 * FilterManager.js - FilterManager
 * Stub temporário
 */

(function(window) {
    'use strict';

    const KC = window.KnowledgeConsolidator;

    class FilterManager {
        constructor() {
            // Stub
        }

        initialize() {
            console.log('FilterManager inicializado (stub)');
        }
    }

    KC.FilterManager = new FilterManager();

})(window);
