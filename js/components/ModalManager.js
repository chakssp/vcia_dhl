/**
 * ModalManager.js - ModalManager
 * Stub temporário
 */

(function(window) {
    'use strict';

    const KC = window.KnowledgeConsolidator;

    class ModalManager {
        constructor() {
            // Stub
        }

        initialize() {
            console.log('ModalManager inicializado (stub)');
        }
    }

    KC.ModalManager = new ModalManager();

})(window);
