/**
 * FileRenderer.js - FileRenderer
 * Stub temporário
 */

(function(window) {
    'use strict';

    const KC = window.KnowledgeConsolidator;

    class FileRenderer {
        constructor() {
            // Stub
        }

        initialize() {
            console.log('FileRenderer inicializado (stub)');
        }
    }

    KC.FileRenderer = new FileRenderer();

})(window);
