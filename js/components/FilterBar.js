/**
 * FilterBar.js - FilterBar
 * Stub temporário
 */

(function(window) {
    'use strict';

    const KC = window.KnowledgeConsolidator;

    class FilterBar {
        constructor() {
            // Stub
        }

        initialize() {
            console.log('FilterBar inicializado (stub)');
        }
    }

    KC.FilterBar = new FilterBar();

})(window);
